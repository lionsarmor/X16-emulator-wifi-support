<resources>
    <!-- app_name is injected at build time via the resValue in build.gradle
         (defaults to "X16 Emulator (WiFi)", overridable with -PxAppLabel=...)
         so each bundled app can show its own name without editing this file. -->
</resources>
